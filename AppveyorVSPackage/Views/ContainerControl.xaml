<UserControl x:Class="memamjome.AppveyorVSPackage.Views.ContainerControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:memamjome.AppveyorVSPackage.Views"
             Background="{DynamicResource VsBrush.Window}"
             Foreground="{DynamicResource VsBrush.WindowText}"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">

    <!--<UserControl.Resources>
        <local:SettingsControl x:Key="SettingsView" />
    </UserControl.Resources>-->
    <Grid>
    	<Grid.RowDefinitions>
    		<RowDefinition Height="25"/>
    		<RowDefinition Height="Auto"/>
    	</Grid.RowDefinitions>
        <StackPanel Grid.Row="1" x:Name="_containerStackPanel">
            <!--<local:SettingsControl/>
            <local:ProjectsControl/>-->
        </StackPanel>
    </Grid>
</UserControl>
